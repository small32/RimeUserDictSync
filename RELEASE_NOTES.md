# v0.2.2 更新说明

- WebDAV 同步改为单压缩包模式，统一使用 `webdav-sync.zip` 上传和下载，减少大量散文件请求。
- 自动兼容旧版散文件；新压缩包上传成功后清理旧文件和目录，无需手动迁移。
- 保留同步文件的修改时间，并增加压缩包路径安全校验。
- 优化大文件上传的内存占用，打包遇到文件读取错误时会立即停止，避免上传不完整数据。

> 升级前建议备份 RIME 用户词库和 WebDAV 数据。WebDAV 目录须由本工具专用，迁移完成后远端仅保留 `webdav-sync.zip`。
