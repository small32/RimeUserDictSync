# 更新说明

**日期**：2026-08-27
**版本号**：0.2.2
**变更类型**：新功能

---

## 一、概述

WebDAV 同步从「逐文件上传/下载」升级为「单个压缩包」模式。同步数据在上传前
打包为 `webdav-sync.zip`，下载时只需获取并解压这一个文件；旧版散文件会在新包
上传成功后自动清理。

## 二、新功能

### 1. 单压缩包同步
- 上传前将 `Sync/WebDAV/` 目录打包为 `webdav-sync.zip`（Deflated 压缩），覆盖上传到 WebDAV。
- 下载时只拉取这一个压缩包，解压到本地镜像目录。
- 压缩包内文件条目**保留原始修改时间**，不影响「按时间同步」的新旧判断。

### 2. 旧数据自动迁移与清理
- 远端只有旧版散文件时，程序自动进入「兼容迁移」模式：先完整下载散文件合并，
  再打包上传，成功后自动清理远端全部散文件/目录，一次同步完成迁移。
- 清理时机严格在**新包上传成功之后**，上传失败或同步中断绝不清理，防止数据丢失。
- 清理失败的项记录到日志并告警，不影响本次同步结果，可稍后手动删除。

### 3. 安全防护
- 解压时校验条目路径，防止 zip-slip（恶意路径逃逸）。
- 远端目录清理只保留 `webdav-sync.zip` 一项，其余全部删除。

## 三、升级与迁移

1. 直接运行新版即可，无需手动处理 WebDAV 数据。
2. 首台设备升级后第一次同步，日志会显示：
   `步骤 2/7：远端为旧版散文件 N 个，进入兼容迁移模式`
3. 同步完成后，WebDAV 远端目录应只剩 `webdav-sync.zip`。
4. 其余设备下次同步自动切换到单包模式。

## 四、注意事项

- **WebDAV 目录须专用于本工具**：上传成功后，该目录内除 `webdav-sync.zip` 外
  的其他内容会被自动清理，请勿与其他文件混用。
- 首次升级建议先备份 RIME 用户词库和 WebDAV 数据。
- 若出现「远端既有压缩包又有散文件」（上次清理中断遗留），程序只认压缩包，
  散文件会在本次同步中一并清理。
